name: Hybrid block
group: Block
props:
  type: object
  additionalProperties: false
  properties:
    variant:
      type: string
      title: Variant
      enum:
        - default
        - info
        - primary
      default: default
