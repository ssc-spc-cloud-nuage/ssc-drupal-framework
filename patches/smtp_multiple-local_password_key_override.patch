diff --git a/src/Plugin/Mail/SmtpMultipleMailSystem.php b/src/Plugin/Mail/SmtpMultipleMailSystem.php
index e92e969..4038b13 100644
--- a/src/Plugin/Mail/SmtpMultipleMailSystem.php
+++ b/src/Plugin/Mail/SmtpMultipleMailSystem.php
@@ -96,13 +96,14 @@ class SmtpMultipleMailSystem extends SMTPMailSystem {
       }
 
       if (!empty($entry['smtp_password_key'])) {
-        $key = \Drupal::service('key.repository')->getKey($entry['smtp_password_key']);
-        if ($key) {
-          $password = (string) $key->getKeyValue();
-          if ($password !== '') {
-            $config['smtp_password'] = $password;
-          }
-        }
+        // Pass the override's Key ID through under the same config name
+        // the patched smtp module reads; it resolves the Key itself when
+        // the password is actually needed. This assumes smtp has been
+        // patched to require the Key module (see
+        // https://www.drupal.org/project/smtp/issues/3134249) rather than
+        // storing a plain-text smtp_password, which this site requires.
+        // @see \Drupal\smtp\Plugin\Mail\SMTPMailSystem::getPassword()
+        $config['smtp_password_key'] = $entry['smtp_password_key'];
       }
 
       // Addresses are validated to be unique when saved; use the first
