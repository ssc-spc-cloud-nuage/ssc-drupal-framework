Simple module to add an icon linked to the masquerade action for any link to
user/[uid] with the class masquerade-link added to it.

There is a permission: 'insert masquerade link icon' which allows inserting the icon.
